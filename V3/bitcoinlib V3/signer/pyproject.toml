[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "bitcoin-signer"
version = "0.1.0"
requires-python = ">=3.10"

[tool.maturin]
module-name = "bitcoin_signer"
features = ["pyo3/extension-module"]
